read ARCHITECTURE.md to understand the structure of the program. if you make changes that affect the architecture and the behavior of the program, update ARCHITECTURE.md accordingly.
for python work in asset_generation, always use a local virtual environment at asset_generation/.venv and install/run python packages with that interpreter.
